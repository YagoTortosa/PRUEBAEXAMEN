package FUNCIONES;

public class funcionUno {
    public static boolean puedeSubir(boolean problemaCardiacos, int edad, double altura) {

        /*if (problemaCardiacos) {
            throw new problemaCardiaco("No se permite subir a gente con problemas cardíacos");
        }

        if (edad < 12) {
            throw new edadMinima("La edad mínima son 12");
        }

        if (altura < 1.4) {
            throw new alturaMinima("La altura mínima son 1,40 metros");
        }
        */
        return true;
    }
}
